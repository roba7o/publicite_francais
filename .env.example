# =============================================================================
# French News Scraper Environment Configuration
# =============================================================================
# Copy this file to .env and update with your actual values
# Never commit .env to version control!

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=french_news
POSTGRES_USER=news_user
POSTGRES_PASSWORD=your_secure_password_here

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
# Enable debug logging (true/false)
DEBUG=true

# Offline mode - use test data instead of live scraping (true/false)
OFFLINE=true

# Database schema selection is automatic:
# - TEST_MODE=true (set by make test* commands) → news_data_test  
# - TEST_MODE=false (set by make run) → news_data_dev
# Override schema names if needed:
# NEWS_DATA_TEST_SCHEMA=custom_test_schema
# NEWS_DATA_DEV_SCHEMA=custom_dev_schema