# =============================================================================
# French News Scraper Environment Configuration
# =============================================================================
# Copy this file to .env and update with your actual values
# Never commit .env to version control!

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=french_news_db
POSTGRES_USER=news_user
POSTGRES_PASSWORD=your_secure_password_here

# Test database configuration
POSTGRES_PORT_TEST=5433
POSTGRES_DB_TEST=french_news_test_db

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
# Enable debug logging (true/false)
DEBUG=true

# Test mode controls application behavior:
# - TEST_MODE=true → uses test HTML files + test database
# - TEST_MODE=false → uses live scraping + dev database